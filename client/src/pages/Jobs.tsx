import { JobCard } from "@/components/JobCard";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { trpc } from "@/lib/trpc";
import { ChevronLeft, ChevronRight, Search, SlidersHorizontal } from "lucide-react";
import React, { useEffect, useMemo, useState } from "react";

export default function Jobs() {
  const [query, setQuery] = useState(""); const [debouncedQuery, setDebouncedQuery] = useState(""); const [role, setRole] = useState(""); const [location, setLocation] = useState(""); const [skills, setSkills] = useState(""); const [category, setCategory] = useState(""); const [workMode, setWorkMode] = useState<"all" | "remote" | "hybrid" | "onsite">("all"); const [employmentType, setEmploymentType] = useState<"all" | "internship" | "full_time">("all"); const [experience, setExperience] = useState<"all" | "student" | "entry" | "mid" | "senior">("all"); const [sort, setSort] = useState<"latest" | "title">("latest"); const [page, setPage] = useState(1);
  useEffect(() => { const timeout = window.setTimeout(() => setDebouncedQuery(query.trim()), 320); return () => window.clearTimeout(timeout); }, [query]);
  const params = useMemo(() => ({ query: debouncedQuery || undefined, role: role || undefined, location: location || undefined, skills: skills || undefined, category: category || undefined, workMode: workMode === "all" ? undefined : workMode, employmentType: employmentType === "all" ? undefined : employmentType, experienceLevel: experience === "all" ? undefined : experience, sort, page, pageSize: 9 }), [debouncedQuery, role, location, skills, category, workMode, employmentType, experience, sort, page]);
  const jobs = trpc.jobs.list.useQuery(params);
  const clearFilters = () => { setQuery(""); setDebouncedQuery(""); setRole(""); setLocation(""); setSkills(""); setCategory(""); setWorkMode("all"); setEmploymentType("all"); setExperience("all"); setSort("latest"); setPage(1); };
  const totalPages = Math.max(1, Math.ceil((jobs.data?.total ?? 0) / 9));
  return <div className="space-y-6"><div><p className="eyebrow text-[#27806f]">Discover opportunities</p><h2 className="mt-2 font-display text-3xl font-semibold tracking-[-.05em] text-[#15365c] sm:text-4xl">Explore jobs with intent.</h2><p className="mt-3 max-w-2xl text-sm leading-6 text-[#6a7d92]">Search active roles, save promising opportunities, and return to profile-led recommendations when you want a calculated relevance view.</p></div><section className="rounded-2xl border border-[#e0e8f0] bg-white p-4 sm:p-5"><div className="grid gap-3 lg:grid-cols-3"><div className="relative lg:col-span-2"><Search className="pointer-events-none absolute left-3 top-3 h-4 w-4 text-[#7c8fa2]" /><Input className="h-10 rounded-xl border-[#dce5ed] pl-9" value={query} onChange={event => { setQuery(event.target.value); setPage(1); }} placeholder="Search title, company, location, or a required skill" /></div><Select value={sort} onValueChange={value => { setSort(value as typeof sort); setPage(1); }}><SelectTrigger className="h-10 rounded-xl"><SelectValue /></SelectTrigger><SelectContent><SelectItem value="latest">Newest first</SelectItem><SelectItem value="title">Title A–Z</SelectItem></SelectContent></Select><Input className="h-10 rounded-xl border-[#dce5ed]" value={role} onChange={event => { setRole(event.target.value); setPage(1); }} placeholder="Role title" /><Input className="h-10 rounded-xl border-[#dce5ed]" value={location} onChange={event => { setLocation(event.target.value); setPage(1); }} placeholder="Location" /><Input className="h-10 rounded-xl border-[#dce5ed]" value={skills} onChange={event => { setSkills(event.target.value); setPage(1); }} placeholder="Required skill" /><Input className="h-10 rounded-xl border-[#dce5ed]" value={category} onChange={event => { setCategory(event.target.value); setPage(1); }} placeholder="Category" /><Select value={workMode} onValueChange={value => { setWorkMode(value as typeof workMode); setPage(1); }}><SelectTrigger className="h-10 rounded-xl"><SelectValue placeholder="Work mode" /></SelectTrigger><SelectContent><SelectItem value="all">Any work mode</SelectItem><SelectItem value="remote">Remote</SelectItem><SelectItem value="hybrid">Hybrid</SelectItem><SelectItem value="onsite">On-site</SelectItem></SelectContent></Select><Select value={employmentType} onValueChange={value => { setEmploymentType(value as typeof employmentType); setPage(1); }}><SelectTrigger className="h-10 rounded-xl"><SelectValue placeholder="Job type" /></SelectTrigger><SelectContent><SelectItem value="all">Any job type</SelectItem><SelectItem value="full_time">Full-time</SelectItem><SelectItem value="internship">Internship</SelectItem></SelectContent></Select><Select value={experience} onValueChange={value => { setExperience(value as typeof experience); setPage(1); }}><SelectTrigger className="h-10 rounded-xl"><SelectValue placeholder="Experience" /></SelectTrigger><SelectContent><SelectItem value="all">Any level</SelectItem><SelectItem value="student">Student</SelectItem><SelectItem value="entry">Entry level</SelectItem><SelectItem value="mid">Mid level</SelectItem><SelectItem value="senior">Senior</SelectItem></SelectContent></Select><Button variant="outline" className="h-10 rounded-xl border-[#d8e3ed] font-bold text-[#5c7690]" onClick={clearFilters}><SlidersHorizontal className="mr-2 h-4 w-4" />Reset</Button></div></section>{jobs.isError ? <div className="rounded-3xl border border-[#f1d4d0] bg-white p-8 text-center"><h3 className="font-extrabold text-[#8d4e47]">Jobs could not be loaded.</h3><p className="mt-2 text-sm text-[#78899c]">Please check your connection and try again.</p><Button variant="outline" className="mt-5 rounded-xl" onClick={() => jobs.refetch()}>Try again</Button></div> : jobs.isLoading ? <div className="grid gap-4 lg:grid-cols-2 xl:grid-cols-3">{Array.from({ length: 6 }).map((_, index) => <div key={index} className="h-72 animate-pulse rounded-2xl bg-white" />)}</div> : jobs.data?.items.length ? <><div className="flex items-center justify-between"><p className="text-sm font-semibold text-[#718398]">{jobs.data.total} available role{jobs.data.total === 1 ? "" : "s"}</p><p className="text-sm text-[#8595a6]">Page {jobs.data.page} of {totalPages}</p></div><div className="grid gap-4 lg:grid-cols-2 xl:grid-cols-3">{jobs.data.items.map(job => <JobCard key={job.id} job={job} />)}</div><div className="flex justify-center gap-2"><Button variant="outline" size="icon" className="rounded-lg" disabled={page === 1} onClick={() => setPage(current => current - 1)}><ChevronLeft className="h-4 w-4" /></Button><Button variant="outline" size="icon" className="rounded-lg" disabled={page >= totalPages} onClick={() => setPage(current => current + 1)}><ChevronRight className="h-4 w-4" /></Button></div></> : <div className="rounded-3xl border border-dashed border-[#cfdae5] bg-white px-6 py-16 text-center"><div className="mx-auto grid h-12 w-12 place-items-center rounded-xl bg-[#eef5fc] text-[#3c75a9]"><Search className="h-5 w-5" /></div><h3 className="mt-5 text-lg font-extrabold tracking-[-.035em] text-[#294864]">No matching opportunities yet.</h3><p className="mx-auto mt-2 max-w-md text-sm leading-6 text-[#718398]">Try a broader search, or return when a job manager adds a new active role.</p><Button variant="outline" className="mt-5 rounded-xl border-[#d2dfe9] font-bold text-[#396788]" onClick={clearFilters}>Clear filters</Button></div>}</div>;
}
