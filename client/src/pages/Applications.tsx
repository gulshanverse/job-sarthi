import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { trpc } from "@/lib/trpc";
import { BriefcaseBusiness, MapPin } from "lucide-react";
import { toast } from "sonner";
import { useLocation } from "wouter";

const statusStyle: Record<string, string> = { saved: "bg-[#eef4fa] text-[#58738d]", applied: "bg-[#ebf3fe] text-[#376ca1]", interviewing: "bg-[#f7f0e0] text-[#8b641b]", offer: "bg-[#e5f5ee] text-[#247360]", rejected: "bg-[#fbeeed] text-[#a25752]" };
export default function Applications() {
  const [, setLocation] = useLocation();
  const applications = trpc.jobs.applications.useQuery(); const utils = trpc.useUtils(); const changeStatus = trpc.jobs.setApplicationStatus.useMutation({ onSuccess: async () => { await utils.jobs.applications.invalidate(); toast.success("Application status updated."); }, onError: error => toast.error(error.message) });
  return <div className="space-y-6"><div><p className="eyebrow text-[#27806f]">Stay organised</p><h2 className="mt-2 font-display text-3xl font-semibold tracking-[-.05em] text-[#15365c] sm:text-4xl">Your application journey.</h2><p className="mt-3 text-sm leading-6 text-[#6a7d92]">Track the roles you’re pursuing from saved to offer. Update the stage whenever your process moves forward.</p></div>{applications.isError ? <div className="rounded-3xl border border-[#f0d5d1] bg-white p-8 text-center"><h3 className="font-extrabold text-[#8d4e47]">Applications could not be loaded.</h3><p className="mt-2 text-sm text-[#74869a]">Please try again in a moment.</p><Button variant="outline" className="mt-5 rounded-xl" onClick={() => applications.refetch()}>Try again</Button></div> : applications.isLoading ? <div className="h-72 animate-pulse rounded-3xl bg-white" /> : applications.data?.length ? <div className="overflow-hidden rounded-3xl border border-[#e0e8f0] bg-white">{applications.data.map(({ application, job }) => <div className="flex flex-col gap-4 border-b border-[#edf1f5] p-5 last:border-0 sm:flex-row sm:items-center sm:justify-between sm:p-6" key={application.id}><div className="flex min-w-0 items-center gap-4"><div className="grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-[#eef4fa] text-[#376a9c]"><BriefcaseBusiness className="h-4 w-4" /></div><div className="min-w-0"><p className="truncate font-extrabold text-[#264764]">{job.title}</p><p className="mt-1 flex items-center gap-1.5 text-sm text-[#75879b]"><span>{job.company}</span><span>·</span><MapPin className="h-3 w-3" /><span>{job.location}</span></p></div></div><div className="flex items-center gap-3"><Badge className={`${statusStyle[application.status]} capitalize hover:${statusStyle[application.status]}`}>{application.status}</Badge><Select value={application.status} onValueChange={value => changeStatus.mutate({ jobId: job.id, status: value as "saved" | "applied" | "interviewing" | "offer" | "rejected" })}><SelectTrigger className="h-9 w-[138px] rounded-lg"><SelectValue /></SelectTrigger><SelectContent><SelectItem value="saved">Saved</SelectItem><SelectItem value="applied">Applied</SelectItem><SelectItem value="interviewing">Interviewing</SelectItem><SelectItem value="offer">Offer</SelectItem><SelectItem value="rejected">Rejected</SelectItem></SelectContent></Select></div></div>)}</div> : <div className="rounded-3xl border border-dashed border-[#cedce8] bg-white px-6 py-16 text-center"><div className="mx-auto grid h-12 w-12 place-items-center rounded-xl bg-[#eef5fc] text-[#3c75a9]"><BriefcaseBusiness className="h-5 w-5" /></div><h3 className="mt-5 text-lg font-extrabold tracking-[-.035em] text-[#294864]">Nothing to track yet.</h3><p className="mx-auto mt-2 max-w-md text-sm leading-6 text-[#718398]">When you’re interested in a role, add it to your application journey and update the stage as your process unfolds.</p><Button variant="outline" className="mt-5 rounded-xl border-[#d2dfe9] font-bold text-[#396788]" onClick={() => setLocation("/jobs")}>Explore jobs</Button></div>}</div>;
}
