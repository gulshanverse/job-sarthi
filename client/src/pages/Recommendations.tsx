import { JobCard } from "@/components/JobCard";
import { Button } from "@/components/ui/button";
import { trpc } from "@/lib/trpc";
import { ArrowRight, Lightbulb, Loader2, Sparkles } from "lucide-react";
import { toast } from "sonner";
import { useLocation } from "wouter";

export default function Recommendations() {
  const [, setLocation] = useLocation(); const profile = trpc.profile.get.useQuery(); const list = trpc.recommendations.list.useQuery(); const utils = trpc.useUtils();
  const generate = trpc.recommendations.generate.useMutation({ onSuccess: async () => { await utils.recommendations.list.invalidate(); toast.success("Your recommendations are ready."); }, onError: error => toast.error(error.message) });
  const confirmed = profile.data?.profileConfirmed;
  const items = list.data ?? [];
  if (profile.isError || list.isError) return <div className="rounded-3xl border border-[#f0d5d1] bg-white p-8 text-center"><h2 className="font-extrabold text-[#8d4e47]">Recommendations could not be loaded.</h2><p className="mt-2 text-sm text-[#74869a]">Please try again in a moment.</p><Button variant="outline" className="mt-5 rounded-xl" onClick={() => { profile.refetch(); list.refetch(); }}>Try again</Button></div>;
  return <div className="space-y-6"><section className="rounded-3xl bg-[#0b2a52] p-6 text-white sm:p-8"><div className="flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div className="max-w-2xl"><p className="eyebrow text-[#9edacb]">Your profile, in context</p><h2 className="mt-3 font-display text-3xl font-semibold tracking-[-.05em] sm:text-4xl">Roles that make sense for where you are.</h2><p className="mt-3 text-sm leading-6 text-[#c6d6e5]">We rank available roles from the experience, skills, and preferences you’ve confirmed—and show the reasoning clearly.</p></div><Button className="pressable shrink-0 rounded-xl bg-[#d9f2eb] font-bold text-[#174854] hover:bg-white" disabled={!confirmed || generate.isPending} onClick={() => generate.mutate()}>{generate.isPending ? <Loader2 className="mr-2 h-4 w-4 animate-spin" /> : <Sparkles className="mr-2 h-4 w-4" />}{items.length ? "Refresh matches" : "Generate matches"}</Button></div></section>{!confirmed ? <div className="rounded-3xl border border-dashed border-[#cedce8] bg-white p-8 text-center"><div className="mx-auto grid h-12 w-12 place-items-center rounded-xl bg-[#eef5fc] text-[#3a74a7]"><Lightbulb className="h-5 w-5" /></div><h3 className="mt-5 text-lg font-extrabold tracking-[-.035em] text-[#284864]">Your profile comes first.</h3><p className="mx-auto mt-2 max-w-md text-sm leading-6 text-[#728499]">Confirm the skills, preferences, and experience you want Job Sarthi to use. That keeps every match grounded in your own details.</p><Button className="pressable mt-5 rounded-xl bg-[#0d2c58] font-bold hover:bg-[#12386d]" onClick={() => setLocation("/onboarding")}>Complete profile <ArrowRight className="ml-2 h-4 w-4" /></Button></div> : list.isLoading ? <div className="grid gap-4 lg:grid-cols-2 xl:grid-cols-3">{Array.from({ length: 3 }).map((_, index) => <div className="h-72 animate-pulse rounded-2xl bg-white" key={index} />)}</div> : items.length ? <><div className="flex items-end justify-between"><div><p className="eyebrow text-[#71859b]">Explained relevance</p><h3 className="mt-1 text-xl font-extrabold tracking-[-.04em] text-[#214260]">Your current strongest matches</h3></div><p className="text-sm text-[#73869a]">{items.length} role{items.length === 1 ? "" : "s"} ranked</p></div><div className="grid gap-4 lg:grid-cols-2 xl:grid-cols-3">{items.map(item => <JobCard key={item.job.id} job={{ ...item.job, score: item.recommendation.score, explanation: item.recommendation.explanation, matchingSkills: item.recommendation.matchingSkills, missingSkills: item.recommendation.missingSkills }} />)}</div></> : <div className="rounded-3xl border border-dashed border-[#cedce8] bg-white p-8 text-center"><h3 className="text-lg font-extrabold tracking-[-.035em] text-[#284864]">Ready to look for your fit.</h3><p className="mx-auto mt-2 max-w-md text-sm leading-6 text-[#728499]">Generate recommendations when roles are available. We’ll use your profile and show why each opportunity is worth considering.</p><Button className="pressable mt-5 rounded-xl bg-[#0d2c58] font-bold hover:bg-[#12386d]" disabled={generate.isPending} onClick={() => generate.mutate()}>{generate.isPending ? "Working" : "Generate recommendations"}</Button></div>}</div>;
}
